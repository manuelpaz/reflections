What happens when you initialize a repository? Why do you need to do it?
A folder called .git is created, we need to initialize a repository to track the modifications.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
The stagging area has only the files tha we want to track the working directory has all files and the repository
contains all the history.

How can you use the staging area to make sure you have one commit per logical
change?
The staging area allows to store the files we decide are involve in a logical change and then make the commit 

What are some situations when branches would be helpful in keeping your history
organized? Fixing a bug, adding new feature, when made a differente version of the project.
How would branches help? Branches help separating different versions of project.

How do the diagrams help you visualize the branch structure?
Diagrams help visualing the branch structure allowing watching the points where a change was introduced.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
